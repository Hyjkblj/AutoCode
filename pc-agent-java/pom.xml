<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.autocode</groupId>
        <artifactId>mobile-voice-codeops</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>pc-agent-java</artifactId>
    <name>pc-agent-java</name>

    <dependencies>
        <dependency>
            <groupId>com.autocode</groupId>
            <artifactId>shared-protocol</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.squareup.okhttp3</groupId>
            <artifactId>okhttp</artifactId>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.datatype</groupId>
            <artifactId>jackson-datatype-jsr310</artifactId>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-simple</artifactId>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.4.1</version>
                <configuration>
                    <mainClass>com.autocode.agent.AgentApplication</mainClass>
                    <environmentVariables>
                        <MVP_SANDBOX_SERVER_ENABLED>${env.MVP_SANDBOX_SERVER_ENABLED}</MVP_SANDBOX_SERVER_ENABLED>
                        <MVP_SANDBOX_PORT>${env.MVP_SANDBOX_PORT}</MVP_SANDBOX_PORT>
                        <MVP_ALLOWED_WORKSPACE_PREFIXES>${env.MVP_ALLOWED_WORKSPACE_PREFIXES}</MVP_ALLOWED_WORKSPACE_PREFIXES>
                        <MVP_BASE_URL>${env.MVP_BASE_URL}</MVP_BASE_URL>
                        <MVP_AGENT_TOKEN>${env.MVP_AGENT_TOKEN}</MVP_AGENT_TOKEN>
                        <MVP_NODE_ID>${env.MVP_NODE_ID}</MVP_NODE_ID>
                        <MVP_AGENT_PROFILE>${env.MVP_AGENT_PROFILE}</MVP_AGENT_PROFILE>
                    </environmentVariables>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-assembly-plugin</artifactId>
                <version>3.7.1</version>
                <configuration>
                    <archive>
                        <manifest>
                            <mainClass>com.autocode.agent.AgentApplication</mainClass>
                        </manifest>
                    </archive>
                    <descriptorRefs>
                        <descriptorRef>jar-with-dependencies</descriptorRef>
                    </descriptorRefs>
                </configuration>
                <executions>
                    <execution>
                        <id>make-assembly</id>
                        <phase>package</phase>
                        <goals>
                            <goal>single</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
