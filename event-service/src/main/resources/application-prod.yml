spring:
  datasource:
    url: ${EVENT_DB_URL}
    username: ${EVENT_DB_USER}
    password: ${EVENT_DB_PASSWORD}
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 5000

logging:
  level:
    com.autocode.event: WARN
    org.springframework.web: WARN
