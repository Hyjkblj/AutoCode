apiVersion: v1
kind: Service
metadata:
  name: artifact-service
  namespace: autocode
  labels:
    app: artifact-service
spec:
  selector:
    app: artifact-service
  ports:
    - port: 8081
      targetPort: 8081
      protocol: TCP
  type: ClusterIP
